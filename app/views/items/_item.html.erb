<turbo-frame id="current_item">
  <article class="flex flex-col" id="<%= dom_id item %>">
    <header>
      <%= link_to item.title, item.link %>
    </header>
    <%= item.description.html_safe %>
  </article>
</turbo-frame>
