<%# 02 FEED: the feed view (RDR-01 FeedTree, Feeds mode with a feed
    selected; canvas ModeFeeds). FeedsController#update re-renders the
    inside of this frame (feeds/_view) after a rename or move from it. %>
<turbo-frame id="current_feed" data-action="turbo:frame-render->selection#feedNavigated">
  <%= render "feeds/view" %>
</turbo-frame>
